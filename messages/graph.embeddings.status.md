# summary

Show status of semantic search features and embeddings.

# description

Displays the current state of semantic search infrastructure including:
- Configuration settings (provider, model, feature flags)
- Neo4j connection and vector index support
- Vector index status
- Embedding coverage for objects and fields
- Recommended next steps

# examples

- Show semantic search status:

  <%= config.bin %> <%= command.id %>
