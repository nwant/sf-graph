# summary
Unset a configuration value.

# description
Removes a configuration value, reverting it to its default state or undefined.

# examples
- Unset the OpenAI API key:
  $ sf graph config unset openaiApiKey

- Unset the base URL:
  $ sf graph config unset baseUrl
